# Release Notes Toolkit

Create concise Markdown release-note sections from normalized change records.

## Setup

Run `make setup` to initialize the shared formatting layer and verify the
renderer.

## Test

Run `make test`.
